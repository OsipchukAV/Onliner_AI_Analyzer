# Onliner AI Analyzer
Интеллектуальная система анализа объявлений.
Запуск проекта
1. Установить зависимости: `pip install -r requirements.txt`
2. Создать `.env` с ключом `OPENROUTER_API_KEY`
3. Запустить: `streamlit run web_app.py`
